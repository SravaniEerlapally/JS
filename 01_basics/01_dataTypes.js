"use strict"//treat all js code as newer version
alert(3+3)
//not preferred to write as it is not having a high readability
console.log("hey); console.log(5+2);
let age=20
let state=true
let name="Shami"
/*DATATYPES
   PRIMITIIVE
     NUMBERS->represent both int & float
     BIGINT->greaterthan numbers size we use
     STRINGS->" " & ' '
     BOOLEAN->true,false
     NULL VALUE->when we want to keep any variable empty we assign null .It's a special value
     UNDEFINED VALUE->when defined variable not assigned anything it gives
  NON-PRIMITIVE
     OBJECTS
     SYMBOLS */
console.log(typeof null)//object
console.log(typeof undefined)//undefined
